apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: spring-mongo
  name: spring-mongo
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: spring-mongo
  type: LoadBalancer
status:
  loadBalancer: {}
